<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.aku.hassannaqvi.amanhicovid_19study">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.sections.IdentificationSectionActivity"></activity>
        <activity android:name=".ui.sections.Section06cmActivity" />
        <activity android:name=".ui.sections.Section05cmActivity" />
        <activity android:name=".ui.sections.Section04cmActivity" />
        <activity android:name=".ui.sections.Section03cmActivity" />
        <activity android:name=".ui.sections.Section02cmActivity" />
        <activity android:name=".core.AndroidDatabaseManager" />
        <activity android:name=".ui.EndingActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>